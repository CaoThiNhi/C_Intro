#include<stdio.h>
#define SIZE 7
int main(){

  int a[SIZE]={13, -355, 235, 47, 67, 943, 1222};
  int *ptr;
  int i;

  for(i=0; i<5; i++){
    ptr = &a[i];
    printf("a[%d] = %4d \tptr%d = %p\n", i, *ptr, i, ptr);
  }

  return 0;
}
