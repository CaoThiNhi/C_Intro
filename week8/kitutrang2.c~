#include<stdio.h>

int main(){
  char c;

  while( (c = getchar()) != '\n' ){
    if( c == ' ' )
      {
        continue;
      }else
        putchar(c);
  }

  printf("\nBye Bye\n");

  return 0;
}
